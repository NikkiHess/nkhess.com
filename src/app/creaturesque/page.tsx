import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Creaturesque',
    description: 'Play Creaturesque!',
}

const Creaturesque = () => (
  <div>
    
  </div>
);

export default Creaturesque;